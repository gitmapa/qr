# Generador de Etiquetas

Este desarrollo genera etiquetas (imágenes desde un .png base) con datos extraídos de un servidor postgres.

Cada etiqueta se la nombra con el formato cui_local.png y se las almacena en la carpeta correspondiente a cui (si no existe, se crea).

Fin
